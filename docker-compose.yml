version: "3.9"

services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/data
    environment:
      DATA_FILE: /data/todos.json
      SECRET_KEY: dev
    restart: unless-stopped
